---
export interface Props { items: string[] }
const { items } = Astro.props;
---
<aside class="takeaways" aria-labelledby="takeaways-title"><h2 id="takeaways-title">Key takeaways</h2><ul>{items.map((item) => <li>{item}</li>)}</ul></aside>
